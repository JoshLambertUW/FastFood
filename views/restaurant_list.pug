extends layout

block content
  h1= title
  
  ul
    each restaurant in restaurant_list
      li 
        a(href=restaurant.url) #{restaurant.name}

    else
      p There are no restaurants at this time.